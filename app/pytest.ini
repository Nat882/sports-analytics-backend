# pytest.ini
[pytest]
# only look for tests in the top-level tests/ folder
testpaths = tests

# ensure the project root is on sys.path so `import app` works
pythonpath = .

# don’t recurse into your static assets
norecursedirs = app/static

# quieter output by default
addopts = -q
